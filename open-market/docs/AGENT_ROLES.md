# 🤖 LLM 에이전트 역할 분담 (AGENT_ROLES.md)

> **이 파일의 목적**: 각 LLM 에이전트(Claude Code, Gemini, Codex)의 역할과 작업 범위를 정의합니다.

---

## 역할 개요

```
┌─────────────────────────────────────────────────────────────────┐
│                    Open Market Project                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────────┐        ┌─────────────┐                       │
│   │   Human     │        │    LLM      │                       │
│   │      │        │  Agents     │                       │
│   └──────┬──────┘        └──────┬──────┘                       │
│          │                      │                               │
│          ▼                      ▼                               │
│   ┌─────────────┐   ┌─────────────────────────────────┐        │
│   │  Backend    │   │  Frontend + Infra + Mock        │        │
│   │  (Kotlin)   │   │  (Next.js, Docker, Mock API)    │        │
│   └─────────────┘   └─────────────────────────────────┘        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 👤 Human (조봉준) 담당 영역

### 담당 범위
- **Backend 전체** (Kotlin + Spring Boot)
- 코드 리뷰 및 최종 승인
- 아키텍처 의사결정
- 외부 연동 비즈니스 로직

### 작업 목록
1. Spring Boot 프로젝트 초기 설정
2. 도메인 모델 설계 및 구현
3. API 개발
4. 외부 연동 어댑터 구현
5. 테스트 코드 작성
6. 성능 최적화

### 산출물
- `backend/` 디렉토리 전체
- 백엔드 관련 문서 업데이트

---

## 🤖 LLM Agent 담당 영역

### 공통 규칙

1. **작업 시작 전 필수 확인**
   ```
   1. CONTEXT.md 읽기 - 현재 상태 파악
   2. HISTORY.md 읽기 - 최근 작업 확인
   3. 담당 SPEC 문서 읽기 - 요구사항 확인
   ```

2. **작업 완료 후 필수 업데이트**
   ```
   1. HISTORY.md에 작업 내역 추가
   2. CONTEXT.md 현재 상태 업데이트
   3. 필요시 SPEC 문서 업데이트
   ```

3. **커밋 메시지 형식**
   ```
   [타입] 설명
   
   - 세부 변경사항 1
   - 세부 변경사항 2
   
   Agent: [Claude Code / Gemini / Codex]
   ```

---

## 🖥️ Frontend Agent

### 담당 범위
- Next.js 프론트엔드 전체 개발
- UI/UX 구현
- API 연동

### 참조 문서
- [FRONTEND_SPEC.md](./FRONTEND_SPEC.md)

### 작업 목록

#### Phase 1: 초기 설정
- [ ] Next.js 14 프로젝트 생성 (App Router)
- [ ] TypeScript 설정
- [ ] Tailwind CSS + shadcn/ui 설정
- [ ] ESLint + Prettier 설정
- [ ] 폴더 구조 생성

#### Phase 2: 공통 컴포넌트
- [ ] Layout 컴포넌트 (Header, Footer, Sidebar)
- [ ] 공통 UI 컴포넌트 (Button, Input, Modal, Toast)
- [ ] 인증 관련 컴포넌트 (LoginForm, SignupForm)
- [ ] API 클라이언트 설정 (axios/fetch wrapper)

#### Phase 3: 페이지 개발
- [ ] 구매자 페이지 (메인, 상품목록, 상세, 장바구니, 주문)
- [ ] 판매자 페이지 (대시보드, 상품관리, 주문관리)
- [ ] 공통 페이지 (로그인, 회원가입, 마이페이지)

### 기술 스택
```json
{
  "framework": "Next.js 14+",
  "language": "TypeScript",
  "styling": "Tailwind CSS",
  "components": "shadcn/ui",
  "state": "Zustand",
  "server-state": "React Query (TanStack Query)",
  "form": "React Hook Form + Zod",
  "icons": "Lucide React"
}
```

### 폴더 구조
```
frontend/
├── src/
│   ├── app/                    # App Router
│   │   ├── (auth)/            # 인증 관련 라우트 그룹
│   │   ├── (buyer)/           # 구매자 라우트 그룹
│   │   ├── (seller)/          # 판매자 라우트 그룹
│   │   └── layout.tsx
│   ├── components/
│   │   ├── ui/                # shadcn/ui 컴포넌트
│   │   ├── layout/            # 레이아웃 컴포넌트
│   │   └── features/          # 기능별 컴포넌트
│   ├── hooks/                 # Custom Hooks
│   ├── stores/                # Zustand Stores
│   ├── lib/                   # 유틸리티, API 클라이언트
│   └── types/                 # TypeScript 타입 정의
├── public/
├── package.json
└── tailwind.config.ts
```

---

## 🏗️ Infrastructure Agent

### 담당 범위
- Docker 환경 구성
- CI/CD 파이프라인
- 모니터링 설정
- 클라우드 인프라 (Terraform)

### 참조 문서
- [INFRA_SPEC.md](./INFRA_SPEC.md)

### 작업 목록

#### Phase 1: 로컬 개발 환경
- [ ] docker-compose.yml 작성 (MySQL, Redis, Kafka, ES)
- [ ] 초기화 스크립트 (DB 스키마, 초기 데이터)
- [ ] 개발 환경 README

#### Phase 2: CI/CD
- [ ] GitHub Actions 워크플로우
  - [ ] Backend 빌드/테스트
  - [ ] Frontend 빌드/테스트
  - [ ] Docker 이미지 빌드
- [ ] 환경별 설정 (dev, staging, prod)

#### Phase 3: 모니터링
- [ ] Prometheus 설정
- [ ] Grafana 대시보드
- [ ] 알림 설정

#### Phase 4: 클라우드 (Optional)
- [ ] Terraform AWS 리소스 정의
- [ ] ECS/EKS 배포 설정

### 폴더 구조
```
infra/
├── docker/
│   ├── docker-compose.yml        # 로컬 개발 환경
│   ├── docker-compose.prod.yml   # 프로덕션 환경
│   ├── backend/
│   │   └── Dockerfile
│   ├── frontend/
│   │   └── Dockerfile
│   └── init/
│       ├── mysql/
│       │   └── init.sql
│       └── elasticsearch/
│           └── mapping.json
├── github-actions/
│   └── workflows/
│       ├── backend-ci.yml
│       ├── frontend-ci.yml
│       └── deploy.yml
├── monitoring/
│   ├── prometheus/
│   │   └── prometheus.yml
│   └── grafana/
│       └── dashboards/
└── terraform/
    ├── main.tf
    ├── variables.tf
    └── modules/
```

---

## 🔌 Mock Server Agent

### 담당 범위
- 외부 채널 Mock API 구현
- PG사 Mock API 구현
- 테스트 시나리오 설정

### 참조 문서
- [EXTERNAL_INTEGRATION_SPEC.md](./EXTERNAL_INTEGRATION_SPEC.md)
- [PG_INTEGRATION_SPEC.md](./PG_INTEGRATION_SPEC.md)

### 작업 목록

#### Phase 1: PG Mock Server
- [ ] 토스페이먼츠 Mock API
- [ ] 카카오페이 Mock API
- [ ] 네이버페이 Mock API
- [ ] 다날 Mock API

#### Phase 2: Channel Mock Server
- [ ] 11번가 Mock API
- [ ] 네이버 스마트스토어 Mock API
- [ ] 카카오 스토어 Mock API
- [ ] 토스 스토어 Mock API
- [ ] 쿠팡 스토어 Mock API

### 기술 스택
```json
{
  "framework": "Express.js 또는 Fastify",
  "language": "TypeScript",
  "database": "SQLite (상태 저장용)",
  "docs": "Swagger/OpenAPI"
}
```

### 폴더 구조
```
mock-servers/
├── pg-mock/
│   ├── src/
│   │   ├── routes/
│   │   │   ├── toss-payments/
│   │   │   ├── kakao-pay/
│   │   │   ├── naver-pay/
│   │   │   └── danal/
│   │   ├── services/
│   │   ├── types/
│   │   └── app.ts
│   ├── scenarios/              # 테스트 시나리오 설정
│   └── package.json
│
└── channel-mock/
    ├── src/
    │   ├── routes/
    │   │   ├── st11/           # 11번가
    │   │   ├── naver-store/    # 네이버 스마트스토어
    │   │   ├── kakao-store/
    │   │   ├── toss-store/
    │   │   └── coupang/
    │   ├── services/
    │   └── app.ts
    ├── scenarios/
    └── package.json
```

---

## 🔄 에이전트 간 협업 프로토콜

### 의존성이 있는 작업

```
Frontend ──depends on──> Backend API
                         │
                         ▼
                    API 문서 (OpenAPI)
```

- Frontend Agent는 Backend API 스펙이 확정된 후 API 연동 작업
- API 스펙 변경 시 HISTORY.md에 명시

### 브랜치 전략

```
main
  │
  ├── develop
  │     │
  │     ├── feature/backend/{feature}    # Human
  │     ├── feature/frontend/{feature}   # LLM
  │     ├── feature/infra/{feature}      # LLM
  │     └── feature/mock/{feature}       # LLM
  │
  └── release/{version}
```

### 코드 리뷰
- LLM 작업물은 Human이 최종 리뷰 후 머지
- 리뷰 요청 시 HISTORY.md에 작업 내역 필수 기록

---

## 📋 Agent 작업 체크리스트

### 작업 시작 시
- [ ] CONTEXT.md 읽음
- [ ] HISTORY.md 최근 로그 확인
- [ ] 담당 SPEC 문서 확인
- [ ] 의존성 확인 (다른 Agent 작업 완료 여부)

### 작업 중
- [ ] SPEC 문서의 요구사항 준수
- [ ] 코딩 컨벤션 준수
- [ ] 테스트 코드 작성

### 작업 완료 시
- [ ] HISTORY.md에 작업 로그 추가
- [ ] CONTEXT.md 상태 업데이트
- [ ] 커밋 메시지 형식 준수
- [ ] 다음 작업 명시
